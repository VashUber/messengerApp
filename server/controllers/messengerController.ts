import { Request, Response } from "express"

const messengerController = {
  async getChats(req: Request, resp: Response) {},

  async getMesseges(req: Request, resp: Response) {},
}

export default messengerController
